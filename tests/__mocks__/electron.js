'use strict';
// Minimal Electron stub — enough for unit tests that import main-process modules.
const app = {
  getPath: () => '/tmp/prateek-term-test',
  isPackaged: false,
};
module.exports = { app };
